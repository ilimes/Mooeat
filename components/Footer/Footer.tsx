'use client'

import { Layout, Row, Col } from 'antd';

const { Footer } = Layout;

const FooterPage = () => {
    return (
        <Footer>
          <Row justify={'center'}>
            <Col span={7}>
              dd
            </Col>
          </Row>
        </Footer>
    )
}

export default FooterPage;